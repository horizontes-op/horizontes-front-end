import Image from '../images/logo.png';
const Navbar = () => {
    return (
        <nav>
            
            <div className="links">
                <img src={Image}></img>
            </div>
        </nav>
    );
}

export default Navbar;